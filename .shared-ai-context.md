# myTrading — 專案 AI Context

## 主題與概述

個人交易輔助與複盤練習工具，長期目標是取代 TradingView 並支援自動化交易。
目前階段：K 線瀏覽、手動標記有效前高/前低、力道子圖輔助判讀、回放練習。

---

## 技術棧

| 層 | 技術 |
|----|------|
| 後端 | Python / FastAPI / Uvicorn / Pandas / aiosqlite |
| 前端 | React 18 + TypeScript + Vite + lightweight-charts v5.1.0 |
| 資料庫 | SQLite — `crypto_data.db`（K 線唯讀）+ `marks.db`（標記讀寫）|
| 啟動器 | C# `TradingLauncher.exe`（同時啟動 backend + browser）|

---

## 目錄結構

```
trading/
├── run.py                      ← 後端入口（uvicorn 啟動方式）
├── backend/
│   ├── main.py                 ← FastAPI app + router 註冊
│   ├── db.py                   ← 資料庫路徑設定（只改這裡）
│   ├── core/                   ← 純計算模組（不依賴 FastAPI）
│   │   ├── atr.py / force_analysis.py / displacement.py
│   │   ├── swing_validity.py / candle_quality.py
│   │   ├── adx.py              ← Wilder 平滑 ADX（已實作，UI 暫時隱藏）
│   │   └── live_fetch.py       ← Binance API 拉 K 線
│   └── routes/
│       ├── klines.py / indicators.py / marks.py
│       ├── swings.py / series.py / ranges.py / live.py
├── frontend/src/
│   ├── App.tsx                 ← 主邏輯（狀態 + 回放 + 事件）
│   ├── types.ts                ← 所有 TypeScript 型別
│   ├── api/client.ts           ← 所有後端 API 呼叫
│   ├── components/
│   │   ├── Chart.tsx           ← 主圖（K 線 + 波段 + 橫盤 + 倉位）
│   │   ├── SubChart.tsx        ← 力道/ADX 子圖（mode 切換）
│   │   ├── Toolbar.tsx         ← 頂部工具列（含回放控制）
│   │   ├── MarkPanel.tsx       ← 右側標記面板
│   │   └── PositionPanel.tsx   ← 倉位管理面板
│   ├── hooks/useLiveSync.ts
│   └── replay/                 ← 回放模式相關型別
├── data/
│   ├── crypto_data.db          ← K 線資料（唯讀）
│   └── marks.db                ← 標記（讀寫）
└── launcher/ TradingLauncher.cs
```

---

## 核心功能

| 功能 | 說明 |
|------|------|
| K 線瀏覽 | 滾動載入、跳轉日期、Shift+點擊選區間 |
| 手動標記 | 快捷鍵 H/L 標記有效前高/低（其他標記類型存在但不主動使用）|
| 自動波段 | pivot_n 幾何高低點 + force_ratio / ATR 有效性驗證 |
| 力道子圖 | force_ratio 直方圖 + 位移效率折線，時間軸在最底部，十字線與主圖同步 |
| 橫盤偵測 | 位移效率 < 0.3 持續 ≥ 10 根，藍/黃虛線框標示 |
| 回放模式 | 指定錨點時間，逐根推進，支援倉位與標記記錄，速度 1x/2x/4x/8x |
| 即時同步 | polling 方式從 Binance 補 K 線入 SQLite |
| 倉位工具 | 圖上點選進場、設 TP/SL、記錄出場，TradingView 風格 |

---

## 重大決策記錄

- **子圖同步用 ref 不用 state**：避免捲圖觸發 re-render 卡頓。App 持有 `subChartSetLogicalRef` 和 `subChartSetCrosshairRef`，Chart 直接呼叫。
- **後端必須用 `run.py` 啟動**：FastAPI 相對 import，直接跑 `main.py` 會報 `ImportError`。
- **K 線資料庫路徑在 `backend/db.py`**：`CRYPTO_DB_PATH`，換機器只改這裡。
- **ADX 已實作但 UI 隱藏**：lag 太大（27 根 warmup），對短線參考價值低。按鈕已從 Toolbar 移除，後端程式碼保留。
- **倉位與標記在回放模式下為臨時狀態**：回放結束清除，不寫入 DB。
- **手動標記範圍**：使用者只標記有效前高/低（H/L 鍵），其他標記類型存在但不主動使用。

---

## 進行中任務 / 下一步

- [ ] 子圖與主圖 1 根 K 線時間偏移問題待確認（是柱子錯位？還是時間標籤不同？）
- [ ] PERF-01：回放長時間推進後力道子圖可能偏移
- [ ] PERF-02：高速回放效能優化

---

## 已完成（最近 10 筆）

1. ADX 後端核心（`backend/core/adx.py`）+ series/indicators 端點擴充；ADX UI 按鈕暫時隱藏
2. 子圖十字線與主圖同步（`subscribeCrosshairMove` → `setCrosshairPosition`），右側顯示實際數值（seriesDataRef + modeRef 追蹤）
3. 子圖時間軸移至最底部（主圖開啟子圖時隱藏自身時間軸）
4. 子圖移除固定參考線（0.6/0.5/0.4），改為十字線動態數值
5. 回放模式完整實作（錨點 / 逐步 / 速度控制 / 前向預載）
6. lightweight-charts v4 → v5 遷移
7. 即時同步（live-sync）功能
8. 倉位工具（TradingView 風格）
9. 已關閉交易紀錄高亮顯示
10. `TradingLauncher.exe` C# 啟動器
